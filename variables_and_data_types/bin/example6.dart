void main(List<String> args) {
  const int myAge =
      20; //no need to explicitly specify the data type for const data types unless it's a company/project rule
  const yourAge = 21;
  print(myAge);
  print(yourAge);
}
